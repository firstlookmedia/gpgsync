
machine:
  python:
    version: 3.5.2

dependencies:
  pre:
    - sudo apt-get update ; sudo apt-get install gnupg2
  override:
    - pip install -r ./install/requirements.txt

test:
  override:
    - nosetests
